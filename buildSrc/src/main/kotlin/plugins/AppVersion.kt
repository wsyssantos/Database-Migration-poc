package plugins

object AppVersion {
    private const val major = 1
    private const val minor = 0
    private const val fix = 0
    private const val build = 0

    val versionCode = 1
    val versionName = "${major}.${minor}.${fix}.${build}"
}