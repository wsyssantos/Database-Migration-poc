package dev.wesleysantos.database.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PHRASES".
*/
public class PhrasesDao extends AbstractDao<Phrases, Integer> {

    public static final String TABLENAME = "PHRASES";

    /**
     * Properties of entity Phrases.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Integer.class, "id", true, "ID");
        public final static Property Phrase = new Property(1, String.class, "phrase", false, "PHRASE");
    }

    private DaoSession daoSession;


    public PhrasesDao(DaoConfig config) {
        super(config);
    }
    
    public PhrasesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PHRASES\" (" + //
                "\"ID\" INTEGER PRIMARY KEY ," + // 0: id
                "\"PHRASE\" TEXT);"); // 1: phrase
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PHRASES\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Phrases entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String phrase = entity.getPhrase();
        if (phrase != null) {
            stmt.bindString(2, phrase);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Phrases entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String phrase = entity.getPhrase();
        if (phrase != null) {
            stmt.bindString(2, phrase);
        }
    }

    @Override
    protected final void attachEntity(Phrases entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Integer readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0);
    }    

    @Override
    public Phrases readEntity(Cursor cursor, int offset) {
        Phrases entity = new Phrases( //
            cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // phrase
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Phrases entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0));
        entity.setPhrase(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Integer updateKeyAfterInsert(Phrases entity, long rowId) {
        return entity.getId();
    }
    
    @Override
    public Integer getKey(Phrases entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Phrases entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
